name: generate
description: 生成完整的代码库文档（多文件输出）
arguments:
  - name: path
    type: string
    description: 代码库路径
    required: false
    default: "."
  - name: output
    type: string
    description: 输出目录
    required: false
    default: "docs/deepwiki"
steps:
  - run: echo
    args: ["请提供要生成文档的代码库路径（默认当前目录）："]
